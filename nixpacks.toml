[phases.setup]
nixPkgs = ["nodejs_22", "nodePackages.pnpm"]

[phases.install]
cmds = [
  "pnpm install"
]

[phases.build]
cmds = [
  "pnpm --filter @backstab/server build"
]

[start]
cmd = "pnpm --filter @backstab/server start"