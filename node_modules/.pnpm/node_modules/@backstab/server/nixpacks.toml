[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install -g pnpm", "pnpm install --filter @backstab/server..."]

[phases.build]
cmds = ["pnpm --filter @backstab/server build"]

[start]
cmd = "pnpm --filter @backstab/server start"